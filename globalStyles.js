import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  openSans: {
    fontFamily: 'OpenSans',
    fontWeight: '300',
    fontStyle: 'normal',
  },
});

export default styles;
