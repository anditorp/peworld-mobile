import {
  StyleSheet,
  Text,
  View,
  SafeAreaView,
  Image,
  ScrollView,
} from 'react-native';
import React from 'react';

const WorkerProfile = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text style={{alignItems: 'center'}}>WorkerPrasdasofile</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
};

export default WorkerProfile;

const styles = StyleSheet.create({});
